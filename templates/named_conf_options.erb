options {
    <% if !@forward_ips.empty? %>
      forwarders {
      <% @forward_ips.each do | ip | -%>
        <%= ip %>;
      <% end -%>
      };
    <% end %>

   <% @named_options.each do | named_option | -%>
    <%= named_option %>;
   <% end -%>
    
};
